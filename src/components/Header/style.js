import { makeStyles } from '@material-ui/core/styles'

export default makeStyles((theme) => ({
  header: {
    background: '#343951',
    color: 'white',
    height: 48,
    alignItems: 'center',
    justifyContent: 'center',
  },
}))
